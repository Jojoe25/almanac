{% extends 'base.html.twig' %}

{% block title %} AdminController !{% endblock %}

{% block body %}
 <div class="dashboardnav-container">
    <ul class="dashboardnav-list">


        {% if app.user %}

            {% if is_granted("ROLE_ADMIN") %}
                <li><a href="{{ path("admin_utilisateurs") }}" title="UsersList">Users-List</a></li>
                <li><a href="{{ path("games_delete") }}" title="Delete Games">Supprimer des jeux</a></li>
            {% endif %}

        {% else %}

            <li><a href="{{ path("app_login") }}" title="Login">Login</a></li>
            <li><a href="{{ path("app_register") }}" title="Create account">Register</a></li>

        {% endif %}

    </ul>
 </div>
{% endblock %}

